# .NET
bin/
obj/
*.user
.vs/

# Terraform
.terraform/
# .terraform.lock.hcl is intentionally NOT ignored -- it locks provider versions for reproducible
# `terraform init` across environments (local, LocalStack, CI) and should be committed.
*.tfstate
*.tfstate.*
*.tfvars
!*.tfvars.example
*.tfplan
tfplan*
destroyplan*
crash.log
crash.*.log

# Secrets / local env
.env
.env.local

# OS / editor
.DS_Store
Thumbs.db
